github:
  user: vita-bot

update-submodule:
  SOMns:
    repo:
      url: https://github.com/smarr/SOMns.git
      branch: dev
    submodule:
      path: libs/truffle
      # tags are used to mark accepted versions to keep them available in the repo
      tags: downstream/debugger-%YYYY-%MM-%DD
      repo:
        url: https://github.com/smarr/truffle.git
        branch: debugger/master
      upstream:
        url: https://github.com/oracle/graal.git
        branch: master


